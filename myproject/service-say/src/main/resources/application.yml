server:
  port: 8763

spring:
  application:
    name: service-say

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    status-page-url: http://localhost:${server.port}/swagger-ui.html
